# BA-ParticleTrackingSystem

ffmpeg muss als Paket in installiert werden.
scikit-image(image-reader) muss auch als Paket installiert werden
bokeh muss auch als Paket installiert werden(pip install bokeh)